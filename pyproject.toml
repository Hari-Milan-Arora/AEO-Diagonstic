[project]
name = "aeo-diagnostic"
version = "2.0.0"
requires-python = ">=3.12,<3.15"
dependencies = [
    "fastapi==0.115.0",
    "pydantic==2.9.0",
    "python-dotenv==1.0.1",
    "uvicorn[standard]==0.30.0",
]

[project.scripts]
app = "backend.main:app"
